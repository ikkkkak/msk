/**
 * MapLibre GL + GeoJSON rendering for Habitat Cadastre
 *
 * Uses client-side GeoJSON rendering instead of raster/vector tiles
 * - No tile server complexity
 * - Reliable rendering of 7000+ plots
 * - Instant plot filtering
 * - Professional appearance
 *
 * Disabled for now — @maplibre/maplibre-react-native requires native module setup.
 * Use OSMPlotMapScreen instead for web-based MapLibre GL rendering.
 */

export const USE_HABITAT_MAPLIBRE_GEOJSON = false;
