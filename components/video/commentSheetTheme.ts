import { theme } from "../../theme";

/** Shared theme tokens for video comment sheets + comment rows */
export const CS = {
  primary: theme["color-temporary-primary"] as string,
  primarySoft: theme["color-temporary-primary4"] as string,
  primaryMuted: theme["color-temporary-primary3"] as string,
  surface: "#FFFFFF",
  canvas: "#FAFAFA",
  border: "#EBEBEB",
  text: "#222222",
  muted: "#717171",
  hint: "#B0B0B0",
  chipBg: "#F7F7F7",
  danger: theme["color-danger-500"] as string,
  like: "#ED4956",
  link: "#0095F6",
  linkMuted: "#B2DFFC",
};
